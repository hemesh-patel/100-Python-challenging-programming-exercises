'''
Question 49:

Write a program which can map() to make a list whose elements are square of numbers between 1 and 20 (both included).
'''


import math

numbers =[x for x in range(1,21) if math.sqrt(x)% 1 == 0] # divides each sqrt by 1 to check the remainder
print(numbers)
