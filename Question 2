def factorial(x):
    if x ==0:
        return 1
    elif x>0:
        return x * factorial(x - 1)
    elif x<0:
        return -x * factorial(x+1)

x = int(raw_input('Enter a number: '))
print factorial(x)
